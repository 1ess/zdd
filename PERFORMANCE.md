# 访问性能与新增文章

## 日常构建

首次安装依赖：`npm ci`，然后 `npm ci --prefix themes/journal`。
新增、修改文章或主题后统一执行 `npm run build`。此命令会编译主题、校验足迹与搜索协议、清理旧产物、生成页面并检查站点和链接；GitHub Actions 使用相同入口。

Vercel 部署配置保存在根目录 `vercel.json`：安装命令同时执行根目录及主题目录的 `npm ci`，构建命令为 `npm run build`，输出目录为 `public`。项目 Root Directory 应指向仓库根目录。只安装根目录依赖会导致主题编译报 `Cannot find module 'sass'`；`npm --prefix themes/journal run build` 本身不会自动安装主题依赖。显式指定 npm 可避免根据遗留 `yarn.lock` 自动选择 Yarn。

## CSS 按页面加载（优化项 3）

编辑主题时继续修改 `themes/journal/scss/journal.scss`。
`tools/build-theme-styles.js` 自动生成公共 `journal.css`、正文 `content.css`、地图 `footprints.css` 和搜索 `search.css`。首页/归档只用公共包，文章及普通 Markdown 页面加载正文包，搜索/地图通过页面 Front Matter 标记加载专属包。

拆分依据是组件选择器，不是当前已有文章的 HTML。新增文章里的列表、代码、表格、引用、图片等自动拥有完整正文样式，不需要维护清理白名单。未知自定义选择器保留在公共包；新增一种页面组件时，可在构建脚本中显式增加分类和对应模板引用。

发布 CSS 禁用内嵌 source map。重复规则只有在选择器、条件、声明完全一致时才清理，保留最后一条的位置。构建检查会阻止 source map 再次进入 CSS。

## 搜索按需加载（优化项 9）

`scripts/search-index.js` 从本次 Hexo 发布的文章自动生成索引，文章数量和文章路径均不写死。

- `search-index.json` 是轻量元数据：标题、URL、日期、标签、稳定 ID，加最近 12 篇的短摘要。
- `search/content.<内容指纹>.json` 包含正文搜索文本；用户真正输入关键词后才请求，页面停留、空白输入和中文输入法组合过程中不会触发请求。
- 正文通过文章 URL 派生的稳定 ID 关联，不依赖数组位置。新增/删除/重新排序文章不会把正文绑定到另一篇文章。
- 正文文件带内容指纹，元数据与正文校验版本和 ID。打开旧页面后遇到发布更新，客户端会重新请求元数据并重试一次；网络故障会保留可用结果和重试入口。
- 标题、标签优先于正文命中；保持既有每篇前 500 字符的正文检索范围，不是全文章全文检索。正文不再在第 180 字符处人为插入空格。

新增文章无需手工编辑 JSON、ID、版本或缓存。新增文章是否进入搜索会在构建时检查；`npm run check:search` 还覆盖新增文章、排序、修改正文及跨摘要边界关键词。

## 缓存与验证

CSS/JS 地址按文件内容生成指纹，长时间运行的 `hexo server` 在重新生成时也会刷新指纹。Service Worker 包含所有 CSS 分包及搜索脚本的版本输入，元数据联网优先、指纹正文按需缓存。

本轮以桌面 1920×1080 和移动端 390×844，检查首页、归档、关于、搜索、足迹、技术文章和游记的浅/深色样式。没有重新引入图片基准测试，也没有让验证依赖当前首页文章排列。

# 博客优化建议

> 本文记录检查结论、实施状态和后续建议。优先处理会影响文章可见性、部署稳定性和访问体验的问题。

## 本次已实施

- 发布构建改为先校验足迹、清理 Hexo 缓存、生成页面，再进行冒烟检查；不会再复用旧的文章渲染缓存。
- 新增 `tools/validate-footprints.js`，校验 GeoJSON 结构、经纬度、日期与访问链接字段。
- 新增 `tools/check-site.js`，确认首页、关于、足迹页面存在，并阻止旧的 `lazyload-outer-wrap` 输出重新进入产物。
- 主题 CSS URL 改为稳定地址，恢复浏览器缓存；主题构建配置改为 Dart Sass，避免继续依赖已废弃的 node-sass。
- “关于”页已迁为 `source/about/index.md`，自动沿用主题导航、样式、渐进图片和分享元数据。
- 首页题图、页面题图和文章图片均使用渐进占位；视频保持原生 `src`，不再采用会导致白屏的延迟替换方案。
- 已补充 Open Graph / Twitter 分享元数据、地图加载状态提示与减少动态效果偏好支持。
- 新增 `POST_TEMPLATE.md`，作为新文章、题图和游记结构化信息的统一起点。
- 新增站内链接检查与 GitHub Actions：每次推送和 Pull Request 都会在干净环境编译主题、重新生成站点并执行校验。
- 新增非阻断式媒体质量报告，持续统计图片替代文本、尺寸、原生懒加载，以及视频封面和预加载配置的缺口。

运行方式：日常发布使用 `npm run build`；如果修改了主题 SCSS，先在 `themes/journal` 安装依赖后执行 `npm run build:theme`。

## 当前观察

- 博客由 Hexo 5 和 `journal` 主题构建；根项目和主题各自有一套依赖与构建配置。
- 历史文章的图片曾被 `hexo-lazyload-element` 写入构建缓存，插件移除后，旧缓存仍产出了 `lazyload-outer-wrap` 结构，导致图片无法显示。清理并重新生成后恢复正常。
- 文章图片目前使用浏览器原生懒加载和渐显；视频保持原生加载，避免延迟替换媒体源造成白屏。
- 图床、视频和题图大多引用外部 CDN；内容中存在约 1000 处图片、视频或链接类资源引用。
- “关于”页面是主题内的独立静态 HTML，和 Hexo 模板脱节；样式文件的版本号也在每次渲染时随机变化。

## 优先级清单

| 优先级 | 建议 | 解决的问题 | 成本 |
| --- | --- | --- | --- |
| P0 | 固化“全新构建”流程 | 避免旧缓存让已删除插件继续影响页面 | 低 |
| P0 | 建立媒体资源巡检 | 尽早发现 CDN 图片、视频失效或跨域异常 | 中 |
| P1 | 统一主题样式构建 | 防止 SCSS 与发布的 CSS 不一致 | 中 |
| P1 | 移除 CSS 的随机版本号 | 恢复浏览器缓存，减少重复下载 | 低 |
| P1 | 为图片预留尺寸、为视频提供 poster | 降低页面跳动，改善媒体加载观感 | 中 |
| P2 | 模板化“关于”等静态页 | 导航、样式和元数据可自动同步 | 中 |
| P2 | 规范文章 Front Matter | 改善 SEO、分享卡片和足迹数据维护 | 中 |
| P2 | 为足迹数据增加校验 | 手工新增游记时减少坐标、链接、日期错误 | 低 |
| P3 | 更新依赖和补充自动检查 | 降低维护与安全风险 | 中至高 |

## P0：构建与缓存稳定性

### 问题

Hexo 会把文章处理结果保存在数据库缓存中。若曾启用的渲染插件已移除，但没有清理缓存，旧文章仍可能继续输出旧插件生成的 HTML。这正是历史文章图片出现 `lazyload-outer-wrap`、而较新文章正常的原因。

### 建议

1. 发布前统一执行全新构建：`hexo clean` 后再 `hexo generate`。
2. 将此流程写入唯一的发布脚本；本地预览、CI 和正式发布均调用同一脚本。
3. CI 从干净检出环境构建，不复用 `db.json`、`public` 或旧的插件产物。
4. 在发布说明中记录 Node.js 与 npm 的推荐版本，并提交锁定依赖版本的 lockfile。

### 验收标准

从全新克隆的仓库构建后，历史婚礼文章和新文章都输出普通 `img` 标签，不再出现过期的懒加载包装结构。

## P0：图片与视频资源可靠性

### 风险

大量文章资源依赖外部 CDN。单个资源迁移、过期、响应头变化或跨域策略改变，都可能在历史文章中表现为图片空白、视频无法播放或加载缓慢。

### 建议

1. 定期检查文章中所有图片、视频和题图 URL 的 HTTP 状态、Content-Type、重定向和可访问性。
2. 给图床资源使用不可变文件名和长期缓存策略；更新媒体时上传新文件，而非覆盖旧文件。
3. 关键照片可保留原图备份或第二存储位置，避免单一 CDN 故障。
4. 为媒体失败状态提供清晰的占位和“打开原图/重试”入口。

### 验收标准

每次发布前可得到一份失效媒体列表，且不会因单个资源异常使整篇文章布局崩坏。

## P1：渐进式媒体加载

### 当前策略

文章图片采用原生 `loading="lazy"` 与加载完成后的渐显，兼容性和稳定性较好。视频不再延迟设置 `src`，以避免部分浏览器首次解码时出现长时间白屏。

### 后续建议

1. 图片在 Front Matter 或 Markdown 中记录宽高，或由构建阶段读取图片元数据并输出 `width`、`height` / `aspect-ratio`。这样图片尚未到达时也能保留正确空间，减少布局跳动（CLS）。
2. 为长视频提供 `poster` 封面图，并采用 `preload="metadata"`；首屏视频可保留更积极的加载策略，非首屏视频以封面和用户点击播放为主。
3. 题图可逐步提供 WebP/AVIF 和多尺寸 `srcset`，让手机不必下载桌面原图。
4. 明确例外标记，例如首屏关键图可用 `no-lazy`，避免所有媒体被同一策略处理。

## P1：主题样式与缓存

### 问题

主题同时维护 SCSS 源文件和发布用 CSS，但根目录执行 Hexo 生成时不会自动编译主题 SCSS。两者容易不同步，导致“源码已改、页面却未变化”或临时修改只在本地生效。

此外，主题头部为 CSS 附加随机查询参数，会使每次访问都绕过浏览器缓存。

### 建议

1. 统一成一个构建入口：先编译主题样式，再执行 Hexo 生成。
2. 明确 CSS 的唯一来源：要么只维护编译后的 CSS，要么只维护 SCSS 并在构建时生成 CSS。
3. 用内容哈希或发布版本号替代随机查询参数；文件内容不变时 URL 保持不变，浏览器即可复用缓存。
4. 逐步将旧版 `node-sass` 迁移到 Dart Sass，降低新 Node.js 环境下的安装风险。

## P2：页面结构与内容模型

### 独立静态页

`themes/journal/source/about/index.html` 属于完整的静态 HTML 页面，不会自动继承主题模板的导航、样式版本和 SEO 改动。建议将“关于”、分析等页面迁移为 Hexo 页面（Markdown + layout 或 EJS 模板），让导航中新增“足迹”等改动自动同步。

### Front Matter 协议

建议将文章元数据逐步标准化，尤其是游记：

```yaml
title: 2026 春日京都
date: 2026-04-10
updated: 2026-04-12
description: 京都赏樱与街巷散步记录。
featured_image: https://cdn.example.com/kyoto/cover.webp
featured_image_width: 1600
featured_image_height: 1067
tags:
  - 旅行
travel:
  place: 京都
  coordinates: [135.7681, 35.0116] # GeoJSON 顺序：经度、纬度
  date: 2026-04-10
```

这样可由脚本生成或校验足迹数据，也能统一生成页面描述、分享卡片与题图尺寸。已有文章不必一次性迁移，可在新文章先启用，旧文按需补齐。

## P2：足迹数据维护

当前 GeoJSON 的“城市聚合 + visits 明细”结构适合地图展示和人工编辑。建议在后续增加校验，而非增加复杂格式：

1. 确认每个 Feature 都有名称、有效的经纬度、访问数组和至少一条文章链接。
2. 校验日期格式统一为 `YYYY-MM-DD`，坐标顺序固定为 `[longitude, latitude]`。
3. 校验文章链接能对应生成后的本地页面，避免地图标注跳转 404。
4. 后续可从文章的 `travel` Front Matter 自动生成候选 GeoJSON，再由人工确认城市归并和显示文案。

## P3：质量、性能与可访问性

1. 增加最小发布冒烟检查：全新构建成功、主要页面返回 200、已知历史文章含可见图片、足迹数据可解析。
2. 增加链接与媒体检查，识别站内 404、外链失效和无效锚点。
3. 补充有意义的图片 `alt` 文本；装饰图片可明确使用空 `alt`。地图、弹窗与键盘操作也应具备可访问标签。
4. 为每次主题改动抽样进行桌面与手机视口检查，重点观察首页题图、文章长图、视频、足迹地图和关于页导航。
5. 在正式域名上记录 Lighthouse 基线（LCP、CLS、INP、图片传输量），以数据决定是否需要进一步压缩媒体或拆分资源。

## 推荐实施顺序

### 第一阶段：稳定发布

固化全新构建流程、清理历史插件残留、建立媒体资源巡检，并为图片异常保留可理解的降级表现。

### 第二阶段：加载体验

补齐图片宽高和视频封面，统一主题样式构建，改用稳定的 CSS 版本策略。

### 第三阶段：长期维护

模板化独立静态页、标准化 Front Matter、为足迹添加校验与自动候选生成，再规划 Hexo/主题依赖的升级分支。

## 暂不建议立即做的事

- 不建议再次用 JavaScript 延迟替换视频 `src`；当前已验证这可能让视频在加载或解码阶段呈现白色。
- 不建议一次性重写所有历史文章的图片语法；应先建立兼容规则与检查，再分批迁移。
- 不建议在没有干净构建和页面冒烟检查前直接升级 Hexo 或主题依赖；升级应独立分支进行并逐页回归。
